# Requirements for AMD GPU (DirectML / ROCm)
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
tqdm>=4.65.0
scenedetect[opencv]>=0.6.2

# PyTorch with DirectML (Windows)
torch>=2.1.0
torch-directml>=0.2.1

# ONNX Runtime with DirectML backend
onnxruntime-directml>=1.16.0

# Model loading
spandrel>=0.3.0
